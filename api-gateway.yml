server:
  port: 9000

spring:
  rabbitmq:
    host: 192.168.200.136
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    order-service: /order/**
    product-service: /product/**
  ignored-patterns:
    - /*-service/**
  sensitive-headers:
    -
