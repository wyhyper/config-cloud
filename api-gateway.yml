server:
  port: 9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    order-service: /order/**
    product-service: /product/**
  ignored-patterns:
    - /*-service/**
  sensitive-headers:
    -
